import React from 'react';
import { SearchBar } from '../../containers/SearchBar';

const NavBar = () => (
  <div>
    <div>
      Task App
    </div>
    <SearchBar />
  </div>
);

export { NavBar };
